import { WeftAuthorizationDirective } from './weft-authorization.directive';

describe('WeftAuthorizationDirective', () => {
  it('should create an instance', () => {
    const directive = new WeftAuthorizationDirective();
    expect(directive).toBeTruthy();
  });
});
